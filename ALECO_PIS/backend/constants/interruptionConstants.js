/**
 * Shared constants for power advisories (interruptions).
 * Keep in sync with src/constants/interruptionConstants.js for RESOLVED_DISPLAY_HOURS.
 */

/** Resolved advisories are auto-archived after 1 day 12 hours (36h) from restoration time. */
export const RESOLVED_ARCHIVE_HOURS = 36;
